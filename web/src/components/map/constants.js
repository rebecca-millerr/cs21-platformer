// screen is BLOCKS_ACROSS wide, so BLOCKS_ACROSS + 1 can be shown at once
export const BLOCKS_ACROSS = 16;
export const VISIBLE_BLOCKS_ACROSS = BLOCKS_ACROSS + 1;
export const BLOCKS_DOWN = 12;
export const BLOCK_SIZE = 40;
export const MOVING_SPEED = 16; // bigger means slower

// TODO: have other types/colors dynamically named after builders
export const DEFAULT_BLOCK = '#354585';
export const EMPTY = '#ffffff';
